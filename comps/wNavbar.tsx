import { motion } from "framer-motion";

export default function WNavbar() {
    return (
        <>
            <motion.div className='flex text-2xl w-full h-12 bg-black '
            style={{}}
            >
                class effect
            </motion.div>
        </>
    );
}